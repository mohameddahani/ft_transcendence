# -----------------------------------------------------------------------------
# HTTP -> HTTPS redirect
# All plain HTTP traffic on port 80 is immediately redirected to HTTPS.
# -----------------------------------------------------------------------------

server {
	listen 80;
	listen [::]:80;
	server_name 127.0.0.1;

	location / {
		return 301 https://$host$request_uri;
	}
}